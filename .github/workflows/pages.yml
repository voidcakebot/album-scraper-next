name: Deploy Pages (disabled for dynamic API)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "GitHub Pages deployment is disabled because this app now uses Next.js API routes (/api/scrape)."
          echo "Use a server runtime (e.g. Vercel, Fly.io, Render) for live scraping from the web UI."
